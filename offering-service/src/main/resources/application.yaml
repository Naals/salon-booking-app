spring:
  application:
    name: offering-service
  datasource:
    url: jdbc:mysql://localhost:3306/sb_offer_service_db
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 5004

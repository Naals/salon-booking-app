spring:
  application:
    name: salon-service
  datasource:
    url: jdbc:mysql://localhost:3306/sb_salon_db
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 5002
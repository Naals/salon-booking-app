spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/sb_user_db
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8070/eureka/

keycloak:
  client_secret: "secret_key"
  password: "password"
  username: "asik"
  client-id: "salon-booking-client"

server:
  port: 5001